<nav class="light-blue darken-4">
  <div class="container">
    <div class="nav-wrapper">
      <a href="/dashboard" class="brand-logo center">MusicEvents</a>
      <a href="#" data-activates="main-menu" class="button-collapse show-on-large"><i class="fa fa-bars"></i></a>
      <!-- RIGHT NAV -->
      <ul class="right hide-on-small-only">
        {{#if user}}
        <li><a href="/dashboard">Welcome {{user.name}}</a></li>
        <li><a href="/auth/logout"><i class="fas fa-sign-out-alt"></i></i> Logout</a></li>
        {{/if}}
        {{!-- SIDE MENU --}}
        <ul class="side-nav" id="main-menu">
          {{#if user}}
          <li><a href="/dashboard"><i class="fa fa-cog"></i>Dashboard</a></li>
          <li><a href="/auth/logout"><i class="fas fa-sign-out-alt"></i>Logout</a></li>
          {{else}}
          <li>
            <a class="btn green darken-1" href="/auth/spotify"><i class="fab fa-spotify left"></i> Login With Spotify</a>
          </li>
          {{/if}}
        </ul>
    </div>
  </div>
</nav>